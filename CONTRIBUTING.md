# Contributing

## One type per file

Each `src/**/*.ts` file declares at most one `interface` or `type` (`IMapper`, `IMapperAsync`, `MapperAsyncConcurrencyOptions`). Factory and utility modules contain functions only.

See `docs/adr/0001-one-type-per-file.md`.
